using UnityEngine;
using System.Collections;




public class LocationTradeBuySellBtn : MonoBehaviour
{
    /*
    public enum Trade
    {
        empty,
        buy,
        sell,
        buyAll,
        sellAll
    }

    public LocationCommodityObject parentObject;
    public Trade tradeState;

    void Awake()
    {
        if (parentObject == null) Debug.LogError("ERROR: 'parentObject' not manually set!");
        if (tradeState == Trade.empty) Debug.LogError("ERROR: Trade State not manually set!");
    }

    void OnClick()
    {
        if (tradeState == Trade.buy)
        {
            parentObject.buy();
        }
        else if (tradeState == Trade.sell)
        {
            parentObject.sell();
        }
        else if (tradeState == Trade.buyAll)
        {

        }
        else if (tradeState == Trade.sellAll)
        {

        }
        else Debug.LogWarning("WARNING: no actions on click. (tradeState: " + tradeState + ")");
    }
    */
}
